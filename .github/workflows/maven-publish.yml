name: Build Check

on: [push, workflow_dispatch]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-java@v4
      with:
        java-version: '25'
    - run: mvn clean compile -q
    - run: echo "Success! Ready to add package publishing."
